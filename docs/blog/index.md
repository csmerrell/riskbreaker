# Blog Specs
Two domains:
- clairescott.dev:
    - I already own this one. We'll just need to point it at the new app.

## clairescott.dev
Game Stuff is the primary landing page. I included a bunch of assets that should be enough to decorate with.
        - Copy can all come directly from the steam page.
        - Steam page has:
            - 280 character description.
            - Small intro to the "About this game"
            - 3 gifs, each with topical sub-headers
                - If easy, do a carousel of these w/ copy. Auto-rotate on a slow interval, pause timer if mouse cursor is in the section.
