import { scriptCharacterAnimation } from './characterAnimation';
import { scriptDialogue } from './dialogue';
import { openBattle } from './openBattle';
import { openCamp } from './openCamp';

export const common = {
    scriptCharacterAnimation,
    scriptDialogue,
    openBattle,
    openCamp,
};
